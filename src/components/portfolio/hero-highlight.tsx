/**
 * Empty placeholder for the previous hero highlight implementation.
 * Keeping the module prevents import errors if reintroduced later while
 * ensuring unused animations are not bundled in the current build.
 */

export {};
